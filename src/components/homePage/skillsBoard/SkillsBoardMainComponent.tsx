import Marquee from "react-fast-marquee";

export function SkillsBoardMainComponent() {
    return (
        <Marquee gradient={false} speed={400} loop={2} pauseOnHover>
            aaaaaaaaaaaa
        </Marquee>
    );
}
